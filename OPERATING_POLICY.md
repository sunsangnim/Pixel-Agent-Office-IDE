# Pixel Agent Office IDE 운영정책

## 커밋·푸시 원칙

- 작업 단위(Phase, 간단한 수정·버그픽스 포함)의 검증이 끝나면 추가 확인 없이 Git 커밋 후 `origin/feature/<작업 ID>`에 푸시한다.
- Phase 단위로 진행하는 큰 작업이 아니더라도, 검증(타입체크·빌드·동작 확인)이 끝난 변경은 그때그때 커밋·푸시한다.
- 모든 Phase와 SRS 인수 조건이 끝났을 때만 `main`에 병합하고 `origin/main`에 추가 확인 없이 푸시한다. 기획에 대한 대표 승인 절차는 유지한다.

## 작업 시작 게이트

1. 새 작업마다 전용 작업실의 `프로젝트/<작업 ID>/`에 독립 Git 저장소와 GitHub 비공개 저장소를 생성한다. SRS·PRD·Phase·결과 문서는 저장소 밖 `산출물/문서/<작업 ID>/`에 격리한다. 이어하기와 재시도는 기존 저장소를 유지한다.
2. 저장소 공개 여부와 관계없이 API 키, 인증 정보, 로그인·세션·PTY 기록, 로컬 절대경로, 사용자 작업 문서를 커밋하지 않는다.
3. 에이전트를 실행하기 전에 `SRS-PRD-SCREEN-DESIGN.md`, `PHASES.md`, `README.md` 초안을 생성한다.
4. SRS·PRD·화면설계 통합 문서를 구체화하기 전에는 구현 Phase로 넘어가지 않는다.

## 브랜치 전략 (Git Flow)

- 팀장과 하위 세션은 한 작업에 대해 공용 작업본 하나와 `feature/<작업 ID>` 브랜치 하나를 공유한다. 직원별 `desk/...` 또는 별도 세부 브랜치를 만들지 않는다. 프로젝트 기본 폴더는 최종 병합을 위한 `main` 작업본으로 남긴다.
- IDE는 첫 세션 실행 시 `main`에서 feature 작업본을 만들고, 이후 세션과 이어하기는 같은 작업본을 사용한다. 작업본 준비에 실패하면 에이전트를 `main`에서 대신 실행하지 않는다. 새 저장소 초기화 커밋을 제외한 구현 변경은 feature에만 커밋한다.
- 파일별 담당을 나누고 다른 팀원의 변경을 보존한다. Git 담당 한 명만 커밋·푸시·브랜치 변경·병합을 순차 수행한다. 기본 담당은 배정된 Claude 팀장이며, 없으면 배정된 팀장 중 한 명이다. 다른 팀원은 변경 파일과 검증 결과를 전달한다.
- Phase별 커밋은 feature 브랜치에 쌓고 `origin/<feature-branch>`에 자유롭게 푸시한다.
- 모든 Phase와 SRS 인수 조건이 완료되면 최신 `origin/main`을 feature에 반영하고 충돌 해결 및 재검증을 마친다. 이어서 main 작업본에서 `--no-ff`로 병합하고 `origin/main`에 추가 확인 없이 푸시한다.
- 중간 Phase에서는 main을 병합·푸시하지 않는다. 세션 종료나 직원 퇴근 시에도 공용 작업본과 미커밋 변경을 보존한다. 강제 푸시와 다른 팀원 변경의 폐기는 하지 않는다.

## 에이전트 라우팅

- 사용자가 에이전트를 지정하지 않으면 Claude 팀장이 기본 담당한다.
- “클로드야”, “코덱스야”, “안티그래피야/안티그래비티야”처럼 직접 부르면 해당 팀장이 작업 방법과 하위 세션 사용 여부를 자율 결정한다.
- 사용자가 UI에서 수신자를 직접 선택하면 자동 라우팅보다 우선한다.
- 큰 작업은 사전에 분업 계획을 사용자 채팅에 공지하고 기본적으로 다음처럼 나눈다.
  - Claude: 코딩, 문서작업(SRS, PRD, 화면설계서 통합 문서 생성)
  - Codex: 이미지 생성, 단순 노가다
  - Antigravity: 테스트, 검증

## Phase 실행

1. 한 번에 하나의 Phase만 진행한다.
2. 각 Phase는 구현, 관련 테스트·빌드, 문서 갱신 순서로 완료한다.
3. Phase 완료 시 체크박스와 결과를 기록하고 Git 담당자가 관련 코드·자산만 커밋한 뒤 feature 브랜치에 추가 확인 없이 푸시한다.
4. 실패한 테스트나 미완료 요구사항이 있으면 Phase를 완료 처리하지 않는다.

## 하위 세션과 취합

- 팀장당 하위 세션은 최대 4개다.
- 하위 세션 결과는 팀장이 유휴 상태가 된 뒤 안전하게 전달한다.
- 팀장은 팀 내부 결과의 중복·충돌·누락을 정리한다.
- 큰 작업은 Claude 총괄 코디네이터가 팀별 결과를 최종 통합한다.

## 작업 종료

- 작업 폴더의 `README.md`에 최종 결과물, 실행·사용법, 검증 결과, 변경 이력을 작성한다.
- SRS 인수 조건과 모든 Phase가 완료됐는지 확인한다.
- 비공개 작업 문서를 갱신하고 마지막 코드·자산 변경까지 feature 브랜치에 커밋·푸시한다. "브랜치 전략" 절차대로 `main`에 병합·푸시한 뒤 사용자에게 결과물 경로와 커밋·병합 내역을 전달한다.
