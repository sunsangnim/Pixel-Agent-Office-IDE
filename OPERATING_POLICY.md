# Pixel Agent Office IDE 운영정책

## 커밋·푸시 원칙

- 작업 단위(Phase, 간단한 수정·버그픽스 포함)가 끝나면 확인을 묻지 않고 곧바로 Git 커밋 후 `origin/main`에 푸시한다.
- Phase 단위로 진행하는 큰 작업이 아니더라도, 검증(타입체크·빌드·동작 확인)이 끝난 변경은 그때그때 커밋·푸시한다.

## 작업 시작 게이트

1. 모든 새 작업의 SRS·PRD·Phase·결과 문서는 저장소 밖 앱 전용 비공개 데이터 폴더의 `<timestamp>-<task-name>/` 하위 폴더에서 격리한다.
2. 공개 저장소에는 API 키, 인증 정보, 로그인·세션·PTY 기록, 로컬 절대경로, 사용자 작업 문서를 커밋하지 않는다.
3. 에이전트를 실행하기 전에 `SRS-PRD-SCREEN-DESIGN.md`, `PHASES.md`, `README.md` 초안을 생성한다.
4. SRS·PRD·화면설계 통합 문서를 구체화하기 전에는 구현 Phase로 넘어가지 않는다.

## 브랜치 전략 (Git Flow)

- 각 에이전트(팀장·하위 세션 모두)는 작업을 시작하기 전에 `main`에서 `feature/<task-slug>` 형식의 새 브랜치를 만들고 그 위에서 작업한다. `main`에 직접 커밋하지 않는다.
- 같은 작업을 팀장과 하위 세션이 나눠 맡을 경우, 팀장의 feature 브랜치를 기준으로 하위 세션도 그 브랜치 위에서 이어서 작업하거나, 세부 브랜치를 파서 팀장이 취합할 때 그 feature 브랜치로 병합한다.
- Phase별 커밋은 feature 브랜치에 쌓고 `origin/<feature-branch>`에 자유롭게 푸시한다.
- 모든 Phase와 SRS 인수 조건이 완료되면 feature 브랜치를 `main`에 병합한다. 병합 전 최신 `main`을 받아 충돌을 해결하고, fast-forward보다는 병합 커밋(`--no-ff`)을 남긴다.
- `main`으로의 병합은 작업이 최종 완료된 시점에만 수행한다. 병합·푸시 후 더 이상 필요 없는 feature 브랜치는 정리(삭제)해도 좋다.

## 에이전트 라우팅

- 사용자가 에이전트를 지정하지 않으면 Claude 팀장이 기본 담당한다.
- “클로드야”, “코덱스야”, “안티그래피야/안티그래비티야”처럼 직접 부르면 해당 팀장이 작업 방법과 하위 세션 사용 여부를 자율 결정한다.
- 사용자가 UI에서 수신자를 직접 선택하면 자동 라우팅보다 우선한다.
- 큰 작업은 사전에 분업 계획을 사용자 채팅에 공지하고 기본적으로 다음처럼 나눈다.
  - Claude: 코딩, 문서작업(SRS, PRD, 화면설계서 통합 문서 생성)
  - Codex: 이미지 생성, 단순 노가다
  - Antigravity: 테스트, 검증

## Phase 실행

1. 한 번에 하나의 Phase만 진행한다.
2. 각 Phase는 구현, 관련 테스트·빌드, 문서 갱신 순서로 완료한다.
3. Phase 완료 시 체크박스와 결과를 기록하고 Git 커밋 후 작업 중인 feature 브랜치에 푸시한다.
4. 실패한 테스트나 미완료 요구사항이 있으면 Phase를 완료 처리하지 않는다.

## 하위 세션과 취합

- 팀장당 하위 세션은 최대 4개다.
- 하위 세션 결과는 팀장이 유휴 상태가 된 뒤 안전하게 전달한다.
- 팀장은 팀 내부 결과의 중복·충돌·누락을 정리한다.
- 큰 작업은 Claude 총괄 코디네이터가 팀별 결과를 최종 통합한다.

## 작업 종료

- 작업 폴더의 `README.md`에 최종 결과물, 실행·사용법, 검증 결과, 변경 이력을 작성한다.
- SRS 인수 조건과 모든 Phase가 완료됐는지 확인한다.
- 마지막 문서 변경까지 feature 브랜치에 커밋·푸시하고, "브랜치 전략" 절차대로 `main`에 병합한 뒤 사용자에게 결과물 경로와 커밋·병합 내역을 전달한다.
